# zetanote-localhost

Zetanote localhost APIs example.

## Setup

Download [`nvm`](https://github.com/nvm-sh/nvm) and install Node 18:

```
nvm install 18.15.0
```

Then run:

```
nvm use && npm install
```

## Dev server

To start the dev server, run:

```
nvm use && npm run dev
```
