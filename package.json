{
  "name": "zetanote-localhost",
  "version": "1.0.0",
  "description": "Zetanote localhost APIs example.",
  "scripts": {
    "dev": "NODE_ENV=development ts-node-dev src/index.ts",
    "build": "rm -rf build && tsc",
    "serve": "node build/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.23",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.6.3"
  }
}
